== README

local setup:

* `git clone git@github.com:railsrumble/r13-team-179.git datingscene`

* `cd datingscene`

* Create a `config/database.yml` file with your DB credentials

* `rake db:create`

* `rake db:migrate`

* `rails s puma`


